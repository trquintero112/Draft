const STORE_KEY='fantasy-war-room-2026-v26';
const TABLE=window.SUPABASE_TABLE||'fantasy_players';
let state={players:[],activePos:'ALL'}, sb=null, usingSupabase=false, saveTimer=null, realtimeChannel=null, currentRecommendation=null;
const POSITIONS=['QB','RB','WR','TE','K','DST'];
const $=s=>document.querySelector(s), $$=s=>Array.from(document.querySelectorAll(s));
const rankEditor={active:'ALL',undoStack:[],pressTimer:null,dragging:false,dragId:null};
function uid(name){return String(name).toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')}
function configured(){return window.SUPABASE_URL&&!window.SUPABASE_URL.includes('PASTE_')&&window.SUPABASE_PUBLIC_KEY&&!window.SUPABASE_PUBLIC_KEY.includes('PASTE_')}
async function init(){bind();await connect();render();setupRankEditor();refreshBestRecommendation()}
async function connect(){if(configured()&&window.supabase){sb=window.supabase.createClient(window.SUPABASE_URL,window.SUPABASE_PUBLIC_KEY);usingSupabase=true;await loadFromSupabase();subscribeRealtime();setStatus('Connected to Supabase. Edits sync for everyone.','ok')}else{usingSupabase=false;await loadLocalOrSeed();setStatus('Supabase is not linked yet. Using local browser save only. Update config.js to sync for everyone.','warn')}}
async function loadLocalOrSeed(){const saved=localStorage.getItem(STORE_KEY);if(saved){state=JSON.parse(saved)}else{const data=await(await fetch('data/seed-rankings.json')).json();state.players=normalizeRows(data.players);state.activePos='ALL';localSave()}}
async function loadSeedRows(){const data=await(await fetch('data/seed-rankings.json')).json();return normalizeRows(data.players||[])}
async function loadFromSupabase(){const {data,error}=await sb.from(TABLE).select('*').order('custom_rank',{ascending:true});if(error){setStatus('Supabase error: '+error.message,'bad');await loadLocalOrSeed();return}if(!data||!data.length){await loadLocalOrSeed();setStatus('Supabase connected, table empty. Select Seed Supabase to publish the starter board.','warn');return}const saved=localStorage.getItem(STORE_KEY),old=saved?JSON.parse(saved):{};state.players=normalizeRows(data);state.activePos=old.activePos||'ALL';await mergeSeedRows(true,true);localSave(false)}
function parseInfo(notes){if(!notes)return{};if(typeof notes==='object')return notes;try{return JSON.parse(notes)}catch(e){return{}}}
function normalizeRows(rows){return rows.map(p=>{const info=p.player_info||p.playerInfo||parseInfo(p.notes);return{id:p.id||uid(p.name),name:p.name,team:p.team||'',pos:p.pos||'RB',custom_rank:Number(p.custom_rank??p.rank??info['Consensus Rank']??999),tier:Number(p.tier??99),sources:p.sources||{},player_info:info,notes:p.notes||JSON.stringify(info||{}),drafted:!!p.drafted,draftedBy:p.drafted_by??p.draftedBy??'',pick:p.pick??null,updated_at:p.updated_at||null}})}
function toDb(p){return{id:p.id||uid(p.name),name:p.name,team:p.team,pos:p.pos,custom_rank:Number(p.custom_rank),tier:Number(p.tier),sources:p.sources||{},notes:JSON.stringify(p.player_info||{}),drafted:!!p.drafted,drafted_by:p.draftedBy||'',pick:p.pick||null,updated_at:new Date().toISOString()}}
function bind(){['search','statusFilter','sortBy'].forEach(id=>{const el=$('#'+id);if(el)el.addEventListener('input',()=>{render();refreshBestRecommendation()})});const on=(id,fn)=>{const el=$('#'+id);if(el)el.onclick=fn};on('bestBtn',refreshBestRecommendation);on('chooseRecommendedBtn',draftRecommended);on('resetDraftBtn',resetAllDraft);on('seedSupabaseBtn',seedSupabase);on('refreshSourcesBtn',refreshSourceRankings);on('exportCsvBtn',exportCsv);on('printPdfBtn',()=>window.print());on('editRanksBtn',openRankEditor);on('backToDraftBtn',closeRankEditor);on('undoRankChangeBtn',undoRankChange);on('closePlayerInfoBtn',closePlayerInfo);const imp=$('#importAny');if(imp)imp.onchange=e=>importAny(e.target.files[0]);on('cancelEdit',()=>$('#editDialog').close());const form=$('#editForm');if(form)form.addEventListener('submit',saveEdit);const d=$('#playerInfoDialog');if(d)d.addEventListener('click',e=>{if(e.target===d)closePlayerInfo()})}
function setStatus(msg,cls){const el=$('#syncStatus');if(!el)return;el.textContent=msg;el.className='sync-status '+cls}
function localSave(stamp=true){localStorage.setItem(STORE_KEY,JSON.stringify(state));if(stamp&&$('#lastSaved'))$('#lastSaved').textContent='Saved '+new Date().toLocaleTimeString()}
async function persistPlayer(p){localSave();if(!usingSupabase||!sb)return;clearTimeout(saveTimer);saveTimer=setTimeout(async()=>{const {error}=await sb.from(TABLE).upsert(toDb(p));if(error)setStatus('Supabase save failed: '+error.message,'bad');else setStatus('Saved to Supabase. Everyone will see this update.','ok')},120)}
async function persistMany(players){localSave();if(!usingSupabase||!sb)return;const {error}=await sb.from(TABLE).upsert(players.map(toDb));if(error)setStatus('Supabase bulk save failed: '+error.message,'bad');else setStatus('Bulk save complete in Supabase.','ok')}
async function mergeSeedRows(addMissing=true,mergeSources=true){const seed=await loadSeedRows();const byName=new Map(state.players.map(p=>[p.name.toLowerCase(),p]));seed.forEach(sp=>{let p=byName.get(sp.name.toLowerCase());if(!p&&addMissing){state.players.push(sp);byName.set(sp.name.toLowerCase(),sp)}else if(p){p.sources={...(p.sources||{}),...(sp.sources||{})};p.player_info={...(p.player_info||{}),...(sp.player_info||{})};p.notes=JSON.stringify(p.player_info||{});if(!p.pos)p.pos=sp.pos;if(!p.team)p.team=sp.team}})}
async function seedSupabase(){if(!usingSupabase||!sb){alert('Add your Supabase URL and publishable/anon key in config.js first.');return}await mergeSeedRows(true,true);await persistMany(state.players);await loadFromSupabase();render();refreshBestRecommendation()}
async function refreshSourceRankings(){await mergeSeedRows(true,true);await persistMany(state.players);render();refreshBestRecommendation();setStatus('Source rankings refreshed from draft kit.','ok')}
function subscribeRealtime(){if(!sb)return;if(realtimeChannel)sb.removeChannel(realtimeChannel);realtimeChannel=sb.channel('fantasy-board-changes').on('postgres_changes',{event:'*',schema:'public',table:TABLE},payload=>handleRealtime(payload)).subscribe()}
function handleRealtime(payload){const row=payload.new||payload.old;if(!row)return;const id=row.id;if(payload.eventType==='DELETE')state.players=state.players.filter(p=>p.id!==id);else{const np=normalizeRows([row])[0];const ix=state.players.findIndex(p=>p.id===id);if(ix>-1)state.players[ix]=np;else state.players.push(np)}localSave(false);render();if(!$('#rankEditorView')?.hidden)renderRankEditor();refreshBestRecommendation();setStatus('Live update received from Supabase.','ok')}
function myTeam(){return state.players.filter(p=>p.draftedBy==='Me')}function teamCounts(){return myTeam().reduce((a,p)=>{a[p.pos]=(a[p.pos]||0)+1;return a},{QB:0,RB:0,WR:0,TE:0,K:0,DST:0})}function currentPick(){return state.players.filter(p=>p.drafted).length+1}function sourceAvg(p){const vals=Object.values(p.sources||{}).map(Number).filter(v=>Number.isFinite(v));return vals.length?vals.reduce((a,b)=>a+b,0)/vals.length:p.custom_rank}function posRank(p){const same=state.players.filter(x=>x.pos===p.pos).sort((a,b)=>a.custom_rank-b.custom_rank);return p.pos+(same.findIndex(x=>x.id===p.id)+1)}function availablePlayers(){return state.players.filter(p=>!p.drafted)}
function rosterNeedBonus(p){const c=teamCounts(),pick=currentPick();let bonus=0,reason=[];if(p.pos==='QB'){if(c.QB<1&&pick>55){bonus+=22;reason.push('QB starter still open')}else if(c.QB>=1){bonus-=28;reason.push('QB need already filled')}}if(p.pos==='RB'){if(c.RB<2){bonus+=60;reason.push('need core RB starters')}else if(c.RB<4){bonus+=24;reason.push('RB depth/flex value')}}if(p.pos==='WR'){if(c.WR<2){bonus+=52;reason.push('need core WR starters')}else if(c.WR<5){bonus+=20;reason.push('WR depth/flex value')}}if(p.pos==='TE'){if(c.TE<1){bonus+=25;reason.push('TE starter still open')}else bonus-=18}if(p.pos==='K'){if(c.K<1&&pick>115){bonus+=35;reason.push('late draft K slot open')}else bonus-=95}if(p.pos==='DST'){if(c.DST<1&&pick>105){bonus+=35;reason.push('late draft DST slot open')}else bonus-=90}return{bonus,reason}}
function scarcityBonus(p){const avail=availablePlayers();const sameTierPos=avail.filter(x=>x.pos===p.pos&&x.tier===p.tier).length;const samePos=avail.filter(x=>x.pos===p.pos).length;return{bonus:(sameTierPos<=2?28:sameTierPos<=4?18:sameTierPos<=7?9:0)+(samePos<=8?14:samePos<=14?7:0)+((p.pos==='RB'||p.pos==='WR')?4:p.pos==='TE'?3:0),sameTierPos}}
function recommendationScore(p){const consensus=sourceAvg(p),base=1000-(p.custom_rank*5),consensusEdge=Math.max(-40,Math.min(40,(p.custom_rank-consensus)*2)),tierBoost=Math.max(0,90-(p.tier*8));return Math.round(base+consensusEdge+tierBoost+scarcityBonus(p).bonus+rosterNeedBonus(p).bonus)}
function bestAvailable(){return availablePlayers().sort((a,b)=>recommendationScore(b)-recommendationScore(a)||a.custom_rank-b.custom_rank)[0]||null}
function getFiltered(){const q=$('#search')?.value.trim().toLowerCase()||'',status=$('#statusFilter')?.value||'available';let arr=[...state.players];if(state.activePos!=='ALL')arr=arr.filter(p=>p.pos===state.activePos);if(q)arr=arr.filter(p=>(p.name+' '+p.team+' '+p.pos).toLowerCase().includes(q));if(status==='available')arr=arr.filter(p=>!p.drafted);if(status==='mine')arr=arr.filter(p=>p.draftedBy==='Me');if(status==='drafted')arr=arr.filter(p=>p.drafted);const sort=$('#sortBy')?.value||'custom_rank';arr.sort((a,b)=>sort==='recommendation'?(recommendationScore(b)-recommendationScore(a)||a.custom_rank-b.custom_rank):sort==='tier'?(a.tier-b.tier||a.custom_rank-b.custom_rank):sort==='adp'?(sourceAvg(a)-sourceAvg(b)):sort==='pos'?(a.pos.localeCompare(b.pos)||a.custom_rank-b.custom_rank):a.custom_rank-b.custom_rank);return arr}
function esc(s){return String(s??'').replace(/[&<>"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[m]))}function fmt(n){return Number.isInteger(n)?n:n.toFixed(1)}
function playerInfoHtml(p){const info=p.player_info||{};const rows=[['Consensus Rank',info['Consensus Rank']??p.custom_rank],['Consensus Tier',info['Consensus Tier']??('Tier '+p.tier)],['FantasyPros ECR',info['FantasyPros ECR']??p.sources?.['FantasyPros ECR']??''],['Draft Sharks (3D)',info['Draft Sharks (3D)']??p.sources?.['Draft Sharks (3D)']??''],['Rotoworld Top 200',info['Rotoworld Top 200']??p.sources?.['Rotoworld Top 200']??''],['Avg ADP',info['Avg ADP']??p.sources?.['Avg ADP']??'']];const note=info['Key Player Notes & Analysis']||'';return`<h2>${esc(p.name)}</h2><div class="player-info-sub"><span class="pos ${p.pos}">${esc(p.pos)}</span> <strong>${esc(p.team||'')}</strong></div><div class="player-info-grid">${rows.map(([k,v])=>`<div><span>${esc(k)}</span><strong>${esc(v??'')}</strong></div>`).join('')}</div><div class="player-info-notes"><h3>Key Player Notes & Analysis</h3><p>${esc(note||'No notes loaded for this player.')}</p></div>`}
function showPlayerInfo(id){const p=state.players.find(x=>x.id===id);if(!p)return;$('#playerInfoContent').innerHTML=playerInfoHtml(p);const d=$('#playerInfoDialog');if(d?.showModal)d.showModal();else d?.setAttribute('open','open')}function closePlayerInfo(){const d=$('#playerInfoDialog');if(d?.close)d.close();else d?.removeAttribute('open')}
function render(){renderChips();renderScarcity();const tbody=$('#board tbody');if(!tbody)return;tbody.innerHTML='';for(const p of getFiltered()){const cons=sourceAvg(p);const tr=document.createElement('tr');tr.className=`tier-${Math.min(14,Math.max(1,p.tier))} ${p.drafted?'drafted-row':''}`;tr.innerHTML=`<td data-label="Player"><div class="compact-player-line"><span class="player-name">${esc(p.name)}</span><span class="pos ${p.pos}">${posRank(p)}</span><span class="compact-team">${esc(p.team)}</span></div><div class="meta">${p.drafted?p.draftedBy==='Me'?'On my team':'Drafted by other':'Available'} • hard-press for notes</div><div class="mobile-metrics"><div class="metric-pill"><span>Rank</span><strong>${p.custom_rank}</strong></div><div class="metric-pill"><span>Tier</span><strong>${p.tier}</strong></div><div class="metric-pill"><span>Cons</span><strong>${fmt(cons)}</strong></div><div class="metric-pill"><span>Score</span><strong>${recommendationScore(p)}</strong></div></div></td><td data-label="Custom Ranking"><input class="rank-input" type="number" min="1" value="${p.custom_rank}" data-field="custom_rank" data-id="${p.id}"></td><td data-label="Tier"><input class="tier-input" type="number" min="1" value="${p.tier}" data-field="tier" data-id="${p.id}"></td><td data-label="Consensus"><strong>${fmt(cons)}</strong></td><td data-label="Score"><span class="score-pill">${recommendationScore(p)}</span></td><td data-label="Action"><div class="actions"><button class="mine" data-act="mine" data-id="${p.id}">Mine</button><button class="gone" data-act="gone" data-id="${p.id}">Gone</button><button class="edit" data-act="edit" data-id="${p.id}">Edit</button></div></td>`;tbody.appendChild(tr)}$$('.actions button').forEach(b=>b.onclick=()=>act(b.dataset.act,b.dataset.id));$$('.rank-input,.tier-input').forEach(inp=>inp.onchange=()=>inlineUpdate(inp));renderSidebars();attachDraftBoardInfoPress()}
function attachDraftBoardInfoPress(){document.querySelectorAll('#board td[data-label="Player"]').forEach(cell=>{let timer=null,sx=0,sy=0;const id=cell.closest('tr')?.querySelector('[data-act]')?.dataset.id;if(!id)return;const start=(x,y)=>{sx=x;sy=y;clearTimeout(timer);timer=setTimeout(()=>{cell.classList.add('info-press');showPlayerInfo(id);setTimeout(()=>cell.classList.remove('info-press'),500)},520)};const cancel=()=>{clearTimeout(timer);timer=null};cell.addEventListener('touchstart',e=>{if(e.touches.length===1)start(e.touches[0].clientX,e.touches[0].clientY)},{passive:true});cell.addEventListener('touchmove',e=>{const t=e.touches[0];if(Math.abs(t.clientX-sx)>12||Math.abs(t.clientY-sy)>12)cancel()},{passive:true});cell.addEventListener('touchend',cancel,{passive:true});cell.addEventListener('pointerdown',e=>{if(!('ontouchstart'in window))start(e.clientX,e.clientY)});cell.addEventListener('pointerup',cancel);cell.addEventListener('pointerleave',cancel)})}
function renderChips(){const wrap=$('#positionChips');if(!wrap)return;wrap.innerHTML='';['ALL',...POSITIONS].forEach(pos=>{const b=document.createElement('button');b.className='chip '+(state.activePos===pos?'active':'');b.textContent=pos;b.onclick=()=>{state.activePos=pos;localSave(false);render();refreshBestRecommendation()};wrap.appendChild(b)})}
function renderScarcity(){const wrap=$('#scarcityGrid'),roster=$('#rosterBuild'),avail=availablePlayers(),c=teamCounts();if(roster)roster.innerHTML=`<div class="roster-mode">Current roster</div><div class="roster-counts">QB ${c.QB||0} | RB ${c.RB||0} | WR ${c.WR||0} | TE ${c.TE||0} | K ${c.K||0} | DST ${c.DST||0}</div>`;if(!wrap)return;wrap.innerHTML=POSITIONS.map(pos=>{const list=avail.filter(p=>p.pos===pos);const tiers=[1,2,3,4,5,11,12,13].map(t=>`T${t}: ${list.filter(p=>p.tier===t).length}`).join(' | ');return`<div class="scarcity-tile"><div class="scarcity-pos ${pos}">${pos}</div><div class="scarcity-total">${list.length}</div><div class="scarcity-tiers">${tiers}</div></div>`}).join('')}
function renderSidebars(){const mine=myTeam().sort((a,b)=>(a.pick||999)-(b.pick||999)),drafted=state.players.filter(p=>p.drafted).sort((a,b)=>(a.pick||999)-(b.pick||999));$('#myTeam').innerHTML=mine.map(p=>`<li><strong>${esc(p.name)}</strong> <span class="meta">${p.pos} ${esc(p.team)}, Custom ${p.custom_rank}/Tier ${p.tier}</span></li>`).join('')||'<li class="meta">No picks yet</li>';$('#liveDraftBoard').innerHTML=drafted.map(p=>`<li><span class="pick-num">${p.pick||''}</span> ${esc(p.name)} <span class="meta">${p.pos} ${esc(p.team)} - ${esc(p.draftedBy)}</span></li>`).join('')||'<li class="meta">No picks yet</li>';$('#draftedLog').innerHTML=drafted.slice(-24).reverse().map(p=>`<li>${esc(p.name)} <span class="meta">${esc(p.draftedBy)}</span></li>`).join('')||'<li class="meta">No drafted players yet</li>';$('#availableCount').textContent=availablePlayers().length;$('#myCount').textContent=mine.length;$('#draftedCount').textContent=drafted.length}
function inlineUpdate(inp){const p=state.players.find(x=>x.id===inp.dataset.id);if(!p)return;p[inp.dataset.field]=Number(inp.value);persistPlayer(p);render();refreshBestRecommendation()}function act(action,id){const p=state.players.find(x=>x.id===id);if(!p)return;if(action==='mine'){p.drafted=true;p.draftedBy='Me';p.pick=nextPick()}if(action==='gone'){p.drafted=true;p.draftedBy='Other';p.pick=nextPick()}if(action==='edit')return openEdit(p);persistPlayer(p);render();refreshBestRecommendation()}function nextPick(){return Math.max(0,...state.players.map(p=>p.pick||0))+1}
async function resetAllDraft(){if(!confirm('Reset the entire draft board back to the beginning?'))return;if(!confirm('Final confirmation: reset draft for everyone using Supabase?'))return;state.players.forEach(p=>{p.drafted=false;p.draftedBy='';p.pick=null});await persistMany(state.players);if(usingSupabase&&sb)await loadFromSupabase();render();refreshBestRecommendation();setStatus('Draft reset complete.','ok')}
function refreshBestRecommendation(){const p=bestAvailable();currentRecommendation=p;const box=$('#recommendationBox'),btn=$('#chooseRecommendedBtn');if(!box)return;if(!p){box.textContent='No available players left.';if(btn)btn.hidden=true;return}const need=rosterNeedBonus(p),scarcity=scarcityBonus(p),reasons=[...need.reason,`${scarcity.sameTierPos} ${p.pos}s remain in Tier ${p.tier}`].filter(Boolean);box.innerHTML=`<div class="recommendation-main">${esc(p.name)} <span class="pos ${p.pos}">${p.pos}</span></div><div class="recommendation-meta">${esc(p.team)} | Custom Ranking ${p.custom_rank} | Consensus ${fmt(sourceAvg(p))} | Tier ${p.tier} | Score ${recommendationScore(p)}</div><div class="recommendation-reason">Why: ${reasons.join('; ')||'best combined value by ranking, consensus, tier, and scarcity'}.</div>`;if(btn)btn.hidden=false}function draftRecommended(){if(!currentRecommendation)currentRecommendation=bestAvailable();if(!currentRecommendation){alert('No available players left.');return}act('mine',currentRecommendation.id)}
function openEdit(p){$('#editId').value=p.id;$('#editName').value=p.name;$('#editTeam').value=p.team;$('#editPos').value=p.pos;$('#editRank').value=p.custom_rank;$('#editTier').value=p.tier;$('#editDialog').showModal()}function saveEdit(e){e.preventDefault();const p=state.players.find(x=>x.id===$('#editId').value);if(!p)return;Object.assign(p,{name:$('#editName').value,team:$('#editTeam').value,pos:$('#editPos').value,custom_rank:+$('#editRank').value,tier:+$('#editTier').value});p.id=uid(p.name);persistPlayer(p);$('#editDialog').close();render();refreshBestRecommendation()}
function csvRows(){const rows=[['pick','drafted_by','name','team','pos','custom_rank','consensus_rank','tier','recommendation_score','sources','player_info']];state.players.sort((a,b)=>a.custom_rank-b.custom_rank).forEach(p=>rows.push([p.pick||'',p.draftedBy||'',p.name,p.team,p.pos,p.custom_rank,fmt(sourceAvg(p)),p.tier,recommendationScore(p),JSON.stringify(p.sources||{}),JSON.stringify(p.player_info||{})]));return rows}function exportCsv(){const text=csvRows().map(r=>r.map(v=>`"${String(v).replace(/"/g,'""')}"`).join(',')).join('\n');download(new Blob([text],{type:'text/csv'}),'fantasy-war-room-export.csv')}function download(blob,name){const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;a.click();URL.revokeObjectURL(a.href)}
function importAny(file){if(!file)return;const r=new FileReader();r.onload=async()=>{const text=r.result.trim();try{if(file.name.toLowerCase().endsWith('.json')||text.startsWith('{')||text.startsWith('[')){const obj=JSON.parse(text),incoming=Array.isArray(obj)?obj:(obj.players||[]);if(!incoming.length)throw new Error('No players found in JSON');state.players=normalizeRows(incoming)}else alert('CSV import is still supported for basic ranking columns, but this draft-kit build is JSON/native-first.');await persistMany(state.players);render();refreshBestRecommendation();setStatus('Import complete.','ok')}catch(err){alert('Import failed: '+err.message)}};r.readAsText(file)}
// Ranking editor
function setupRankEditor(){renderRankTabs();updateUndoButton()}function openRankEditor(){document.body.classList.add('edit-rank-mode');const v=$('#rankEditorView');if(v){v.hidden=false;renderRankTabs();renderRankEditor();v.scrollIntoView({behavior:'smooth',block:'start'})}}function closeRankEditor(){document.body.classList.remove('edit-rank-mode');const v=$('#rankEditorView');if(v)v.hidden=true;render();refreshBestRecommendation()}function rankRows(){return[...state.players].filter(p=>rankEditor.active==='ALL'||p.pos===rankEditor.active).sort((a,b)=>a.custom_rank-b.custom_rank||a.name.localeCompare(b.name))}function renderRankTabs(){const tabs=$('#rankEditorTabs');if(!tabs)return;tabs.innerHTML=['ALL',...POSITIONS].map(pos=>`<button class="rank-tab ${rankEditor.active===pos?'active':''}" data-pos="${pos}">${pos}</button>`).join('');tabs.querySelectorAll('[data-pos]').forEach(b=>b.onclick=()=>{rankEditor.active=b.dataset.pos;renderRankTabs();renderRankEditor()})}
function renderRankEditor(){const wrap=$('#rankEditorRows');if(!wrap)return;const rows=rankRows();wrap.innerHTML=rows.map(p=>`<div class="rank-editor-row" data-rank-id="${p.id}"><div class="rank-num"><span class="rank-drag">☰</span><span>${p.custom_rank}</span></div><div class="rank-player-name">${esc(p.name)}</div><div class="rank-team">${esc(p.team||'')}</div><div><input class="rank-tier-input" type="number" min="1" value="${p.tier}" data-tier-id="${p.id}"></div><div><button class="note-btn" type="button" data-info-id="${p.id}" title="View notes">📝</button></div></div>`).join('')||'<div class="rank-editor-empty">No players found.</div>';wrap.querySelectorAll('.rank-editor-row').forEach(row=>{row.addEventListener('touchstart',rankTouchStart,{passive:false,capture:true});row.addEventListener('touchmove',rankTouchMove,{passive:false,capture:true});row.addEventListener('touchend',rankTouchEnd,{passive:false,capture:true});row.addEventListener('touchcancel',rankTouchCancel,{passive:false,capture:true});row.addEventListener('pointerdown',rankPointerStart,{capture:true});row.addEventListener('contextmenu',e=>e.preventDefault())});wrap.querySelectorAll('.rank-tier-input').forEach(i=>{i.addEventListener('touchstart',e=>e.stopPropagation(),{passive:true});i.addEventListener('pointerdown',e=>e.stopPropagation());i.onchange=rankTierChange});wrap.querySelectorAll('.note-btn').forEach(btn=>btn.onclick=e=>{e.preventDefault();e.stopPropagation();showPlayerInfo(btn.dataset.infoId)});updateUndoButton()}
function snapshotRanks(){return state.players.map(p=>({id:p.id,custom_rank:p.custom_rank,tier:p.tier}))}function pushUndo(){rankEditor.undoStack.push(snapshotRanks());if(rankEditor.undoStack.length>20)rankEditor.undoStack.shift();updateUndoButton()}function updateUndoButton(){const b=$('#undoRankChangeBtn');if(b){const has=rankEditor.undoStack.length>0;b.disabled=!has;b.classList.toggle('is-disabled',!has)}}async function undoRankChange(e){if(e){e.preventDefault();e.stopPropagation()}const snap=rankEditor.undoStack.pop();if(!snap){updateUndoButton();return}const map=new Map(snap.map(x=>[x.id,x]));state.players.forEach(p=>{const old=map.get(p.id);if(old){p.custom_rank=old.custom_rank;p.tier=old.tier}});await persistMany(state.players);render();renderRankEditor();refreshBestRecommendation();setStatus('Last ranking edit undone.','ok');updateUndoButton()}
let dragGhost=null,touchState={};function isInteractive(el){return !!el.closest('input,button,select,textarea,.rank-tab,.rank-editor-actions-top')}function rankTouchStart(e){if(e.touches.length!==1||isInteractive(e.target))return;const row=e.currentTarget,t=e.touches[0];touchState={row,startX:t.clientX,startY:t.clientY,active:false,timer:setTimeout(()=>activateDrag(row,t.clientX,t.clientY),320)};row.classList.add('press-arming')}function rankTouchMove(e){if(!touchState.row)return;const t=e.touches[0],dx=Math.abs(t.clientX-touchState.startX),dy=Math.abs(t.clientY-touchState.startY);if(!touchState.active&&(dx>24||dy>24)){clearTimeout(touchState.timer);touchState.row.classList.remove('press-arming');touchState={};return}if(touchState.active){e.preventDefault();moveDrag(t.clientX,t.clientY)}}function rankTouchEnd(e){clearTimeout(touchState.timer);if(touchState.row)touchState.row.classList.remove('press-arming');if(touchState.active){e.preventDefault();finishDrag()}touchState={}}function rankTouchCancel(){rankTouchEnd({preventDefault(){}})}function rankPointerStart(e){if(('ontouchstart'in window)||isInteractive(e.target))return;if(e.button!==undefined&&e.button!==0)return;const row=e.currentTarget;e.preventDefault();activateDrag(row,e.clientX,e.clientY);const move=ev=>{ev.preventDefault();moveDrag(ev.clientX,ev.clientY)},up=()=>{document.removeEventListener('pointermove',move);document.removeEventListener('pointerup',up);finishDrag()};document.addEventListener('pointermove',move,{passive:false});document.addEventListener('pointerup',up,{once:true})}
function activateDrag(row,x,y){if(rankEditor.dragging)return;rankEditor.dragging=true;touchState.active=true;rankEditor.dragId=row.dataset.rankId;pushUndo();if(navigator.vibrate)navigator.vibrate([22]);row.classList.remove('press-arming');row.classList.add('rank-placeholder');document.body.classList.add('rank-dragging');dragGhost=makeGhost(row,x,y);moveGhost(x,y)}function makeGhost(row,x,y){const r=row.getBoundingClientRect(),g=row.cloneNode(true);g.classList.add('rank-drag-ghost');g.style.width=r.width+'px';g.style.left=r.left+'px';g.style.top=r.top+'px';g.dataset.offsetX=x-r.left;g.dataset.offsetY=y-r.top;document.body.appendChild(g);return g}function moveGhost(x,y){if(!dragGhost)return;dragGhost.style.left=(x-Number(dragGhost.dataset.offsetX))+'px';dragGhost.style.top=(y-Number(dragGhost.dataset.offsetY))+'px'}function moveDrag(x,y){moveGhost(x,y);if(y<84)window.scrollBy(0,-18);if(y>window.innerHeight-84)window.scrollBy(0,18);const row=document.querySelector(`.rank-editor-row[data-rank-id="${rankEditor.dragId}"]`);if(!row)return;if(dragGhost)dragGhost.style.display='none';const el=document.elementFromPoint(x,y);if(dragGhost)dragGhost.style.display='';const target=el?.closest?.('.rank-editor-row');if(target&&target!==row&&target.parentNode===row.parentNode){const rect=target.getBoundingClientRect(),before=y<rect.top+rect.height/2;target.parentNode.insertBefore(row,before?target:target.nextSibling)}}async function finishDrag(){const row=document.querySelector(`.rank-editor-row[data-rank-id="${rankEditor.dragId}"]`);if(dragGhost){dragGhost.remove();dragGhost=null}if(row)row.classList.remove('rank-placeholder','press-arming');document.body.classList.remove('rank-dragging');rankEditor.dragging=false;await commitLiveOrder()}async function commitLiveOrder(){const ids=$$('#rankEditorRows .rank-editor-row').map(r=>r.dataset.rankId),vis=rankRows(),slots=vis.map(p=>p.custom_rank).sort((a,b)=>a-b);if(rankEditor.active==='ALL')ids.forEach((id,i)=>{const p=state.players.find(x=>x.id===id);if(p)p.custom_rank=i+1});else ids.forEach((id,i)=>{const p=state.players.find(x=>x.id===id);if(p)p.custom_rank=slots[i]});await persistMany(state.players);render();renderRankEditor();refreshBestRecommendation();setStatus('Custom ranking order saved.','ok')}async function rankTierChange(e){const p=state.players.find(x=>x.id===e.target.dataset.tierId);if(!p)return;pushUndo();p.tier=Number(e.target.value)||p.tier;await persistPlayer(p);render();renderRankEditor();refreshBestRecommendation();setStatus('Tier saved.','ok')}
init();

/* v27 robust draft-kit info hydration: works with existing Supabase rows */
function playerKeyV27(p){return String(p?.id||uid(p?.name||'')).toLowerCase();}
async function mergeSeedRows(addMissing=true,mergeSources=true){
  const seed=await loadSeedRows();
  const byId=new Map(state.players.map(p=>[playerKeyV27(p),p]));
  const byName=new Map(state.players.map(p=>[String(p.name||'').toLowerCase(),p]));
  seed.forEach(sp=>{
    let p=byId.get(playerKeyV27(sp))||byName.get(String(sp.name||'').toLowerCase());
    if(!p&&addMissing){state.players.push(sp);return;}
    if(p){
      p.sources={...(p.sources||{}),...(sp.sources||{})};
      p.player_info={...(parseInfo(p.notes)||{}),...(p.player_info||{}),...(sp.player_info||{})};
      p.notes=JSON.stringify(p.player_info||{});
      p.pos=p.pos||sp.pos;
      p.team=p.team||sp.team;
      if(!Number.isFinite(Number(p.custom_rank))||Number(p.custom_rank)>=900)p.custom_rank=sp.custom_rank;
      if(!Number.isFinite(Number(p.tier))||Number(p.tier)>=90)p.tier=sp.tier;
    }
  });
}
async function loadFromSupabase(){
  const {data,error}=await sb.from(TABLE).select('*').order('custom_rank',{ascending:true});
  if(error){setStatus('Supabase error: '+error.message,'bad');await loadLocalOrSeed();return;}
  if(!data||!data.length){await loadLocalOrSeed();setStatus('Supabase connected, table empty. Select Seed Supabase to publish the starter board.','warn');return;}
  const saved=localStorage.getItem(STORE_KEY),old=saved?JSON.parse(saved):{};
  state.players=normalizeRows(data);state.activePos=old.activePos||'ALL';
  await mergeSeedRows(true,true);
  localSave(false);
  // Persist merged draft-kit info back to Supabase so notes/ECR survive refreshes and other devices.
  await persistMany(state.players);
  setStatus('Draft kit player notes and source rankings loaded into Supabase.','ok');
}
async function refreshSourceRankings(){
  await mergeSeedRows(true,true);
  await persistMany(state.players);
  render();
  if(!$('#rankEditorView')?.hidden)renderRankEditor();
  refreshBestRecommendation();
  setStatus('Draft kit source rankings and player notes refreshed and saved.','ok');
}
async function seedSupabase(){
  if(!usingSupabase||!sb){alert('Add your Supabase URL and publishable/anon key in config.js first.');return;}
  await mergeSeedRows(true,true);
  await persistMany(state.players);
  await loadFromSupabase();
  render();
  if(!$('#rankEditorView')?.hidden)renderRankEditor();
  refreshBestRecommendation();
}
